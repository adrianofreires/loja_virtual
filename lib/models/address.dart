class Address {
  String street, number, complement, district, zipCode, city, state;
  double latitude, longitude;

  Address(
      {this.street,
      this.number,
      this.complement,
      this.district,
      this.zipCode,
      this.city,
      this.state,
      this.latitude,
      this.longitude});
}
